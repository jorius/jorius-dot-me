{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "Launch jorius-web to Chrome",
      "url": "http://localhost:51000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
